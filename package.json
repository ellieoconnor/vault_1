{
    "name": "vault-1",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "build": "npm run build --workspace=apps/frontend",
        "lint": "npm run lint --workspace=apps/frontend",
        "lint:fix": "npm run lint --workspace=apps/frontend -- --fix",
        "format": "prettier --write \"apps/**/*.{ts,tsx,json}\"",
        "format:check": "prettier --check \"apps/**/*.{ts,tsx,json}\"",
        "test": "echo \"Error: no test specified\" && exit 1",
        "test:e2e": "playwright test",
        "test:pact:consumer": "vitest run --config vitest.config.pact.ts",
        "publish:pact": ". ./scripts/env-setup.sh && ./scripts/publish-pact.sh",
        "can:i:deploy:consumer": ". ./scripts/env-setup.sh && PACTICIPANT=vault-1-frontend ./scripts/can-i-deploy.sh",
        "record:consumer:deployment": ". ./scripts/env-setup.sh && PACTICIPANT=vault-1-frontend ./scripts/record-deployment.sh",
        "dev": "concurrently --kill-others-on-fail --names FRONTEND,BACKEND --prefix-colors cyan,yellow \"npm run dev --workspace=apps/frontend\" \"npm run dev --workspace=apps/backend\""
    },
    "workspaces": [
        "apps/*"
    ],
    "repository": {
        "type": "git",
        "url": "git+https://github.com/ellieoconnor/vault_1.git"
    },
    "author": "Elizabeth O'Connor",
    "license": "ISC",
    "bugs": {
        "url": "https://github.com/ellieoconnor/vault_1/issues"
    },
    "homepage": "https://github.com/ellieoconnor/vault_1#readme",
    "description": "",
    "devDependencies": {
        "@pact-foundation/pact": "^16.3.0",
        "concurrently": "^9.0.0",
        "prettier": "^3.8.3"
    }
}
